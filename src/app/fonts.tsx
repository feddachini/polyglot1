import { Zilla_Slab } from "next/font/google";

export const zillaSlab = Zilla_Slab({
  weight: ["400", "500", "700"], // choose the weights you need
  subsets: ["latin"],
  variable: "--font-zilla", // optional: sets a CSS variable
});

